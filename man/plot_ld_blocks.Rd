% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{plot_ld_blocks}
\alias{plot_ld_blocks}
\title{Plot LD Block Structure Across Chromosomes}
\usage{
plot_ld_blocks(
  blocks,
  colour_by = c("length_bp", "CHR"),
  highlight_blocks = NULL,
  mb_scale = TRUE
)
}
\arguments{
\item{blocks}{Data frame of LD blocks (output of
\code{\link{run_Big_LD_all_chr}}). Must contain \code{start.bp},
\code{end.bp}, and \code{CHR}.}

\item{colour_by}{Character. One of \code{"length_bp"} (default, colours by
block size) or \code{"CHR"} (distinct colour per chromosome).}

\item{highlight_blocks}{Optional integer vector of row indices in
\code{blocks} to highlight with a solid border outline.
\code{NULL} (default) highlights nothing.}

\item{mb_scale}{Logical. If \code{TRUE} (default), x-axis is in Megabases.}
}
\value{
A \code{ggplot} object.
}
\description{
Produces a \code{ggplot2}-based overview of LD block boundaries and sizes.
Requires \code{ggplot2} to be installed.
}
\examples{
\donttest{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  blocks <- data.frame(
    CHR      = c("1", "1", "2", "2"),
    start.bp = c(1e5, 6e5, 2e5, 8e5),
    end.bp   = c(5e5, 9e5, 7e5, 1.5e6)
  )
  p <- plot_ld_blocks(blocks)
  print(p)
}
}

}
