% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/haplotype_analysis.R
\name{cv_haplotype_prediction}
\alias{cv_haplotype_prediction}
\title{K-Fold Cross-Validation for Haplotype-Based Genomic Prediction}
\usage{
cv_haplotype_prediction(
  geno_matrix,
  snp_info,
  blocks,
  blues,
  k = 5L,
  n_rep = 1L,
  top_n = NULL,
  min_freq = 0.05,
  min_snps = 3L,
  id_col = "id",
  blue_col = "blue",
  blue_cols = NULL,
  seed = 42L,
  verbose = TRUE
)
}
\arguments{
\item{geno_matrix}{Numeric matrix (individuals x SNPs), MAF-filtered dosage.}

\item{snp_info}{Data frame with columns \code{SNP}, \code{CHR},
\code{POS}.}

\item{blocks}{LD block table from \code{\link{run_Big_LD_all_chr}}.}

\item{blues}{Pre-adjusted phenotype means. Accepts the same four
formats as \code{\link{run_haplotype_prediction}}: named numeric vector,
single-trait data frame, multi-trait data frame, or named list.}

\item{k}{Integer. Number of folds. Default \code{5L}.}

\item{n_rep}{Integer. Number of CV replications (each with a
different random fold assignment). Default \code{1L}.}

\item{top_n}{Integer or \code{NULL}. Maximum haplotype alleles per
block passed to \code{\link{build_haplotype_feature_matrix}}.
Default \code{NULL} (all alleles above \code{min_freq}).}

\item{min_freq}{Numeric. Minimum haplotype allele frequency.
Default \code{0.05}.}

\item{min_snps}{Integer. Minimum SNPs per block for haplotype extraction.
Default \code{3L}.}

\item{id_col}{Character. Name of the individual ID column when
\code{blues} is a data frame. Default \code{"id"}.}

\item{blue_col}{Character. Name of the BLUE column for single-trait data
frames. Default \code{"blue"}.}

\item{blue_cols}{Character vector. Trait column names for multi-trait data
frames. Default \code{NULL} (auto-detect all numeric non-ID columns).}

\item{seed}{Integer. RNG seed for reproducible fold assignment.
Default \code{42L}.}

\item{verbose}{Logical. Print progress. Default \code{TRUE}.}
}
\value{
A named list of class \code{LDxBlocks_cv}:
\describe{
  \item{\code{pa_summary}}{Data frame: \code{trait}, \code{rep},
    \code{fold}, \code{n_train}, \code{n_test}, \code{PA} (Pearson r),
    \code{RMSE}.}
  \item{\code{pa_mean}}{Data frame: mean PA and RMSE per trait across
    all folds and replications.}
  \item{\code{gebv_all}}{Data frame of out-of-fold GEBVs for all
    individuals and traits (one row per individual x trait).}
  \item{\code{k}}{Number of folds used.}
  \item{\code{n_rep}}{Number of replications.}
}
}
\description{
Estimates the predictive ability of the haplotype GBLUP model via k-fold
cross-validation. In each fold, a subset of individuals is masked from the
phenotype and predicted from the haplotype GRM; Pearson correlation between
predicted and observed BLUEs is returned as the predictive ability (PA).
Runs per trait when multiple traits are supplied.
}
\examples{
\donttest{
data(ldx_geno, ldx_snp_info, ldx_blocks, ldx_blues, package = "LDxBlocks")
cv <- cv_haplotype_prediction(
  geno_matrix = ldx_geno,
  snp_info    = ldx_snp_info,
  blocks      = ldx_blocks,
  blues       = ldx_blues,
  k           = 5L,
  id_col      = "id",
  verbose     = FALSE
)
cv$pa_mean
}
}
\seealso{
\code{\link{run_haplotype_prediction}},
  \code{\link{build_haplotype_feature_matrix}}
}
