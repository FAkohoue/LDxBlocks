% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/haplotypes.R
\name{write_haplotype_character}
\alias{write_haplotype_character}
\title{Write Haplotype Character (Nucleotide) Matrix}
\usage{
write_haplotype_character(
  haplotypes,
  snp_info,
  out_file,
  min_freq = 0.01,
  top_n = NULL,
  missing_string = ".",
  verbose = TRUE
)
}
\arguments{
\item{haplotypes}{List from \code{\link{extract_haplotypes}}.}

\item{snp_info}{Data frame with \code{SNP}, \code{CHR}, \code{POS},
\code{REF}, \code{ALT}.}

\item{out_file}{Output file path (tab-delimited).}

\item{min_freq}{Minimum haplotype frequency. Default \code{0.01}.}

\item{top_n}{Integer or \code{NULL}. Cap alleles per block.
\code{NULL} (default) keeps all above \code{min_freq}.}

\item{missing_string}{Missing genotype marker. Default \code{"."}.}

\item{verbose}{Logical. Default \code{TRUE}.}
}
\value{
Invisibly returns \code{out_file}.
}
\description{
Writes a matrix where each cell contains the nucleotide sequence of the
haplotype allele carried by each individual. Rows are haplotype alleles,
columns are individuals. This is the most interpretable format: you can
read directly which nucleotides define each haplotype allele and which
individuals carry it.
}
\details{
The cell value for individual i at haplotype allele h is:
\itemize{
  \item The nucleotide sequence (e.g. \code{"AGTTA"}) if the individual
    carries that allele (dosage = 2 for unphased, or present in either
    gamete for phased).
  \item \code{"-"} if the individual does not carry that allele.
  \item \code{"."} if the individual has missing data in that block.
}
}
